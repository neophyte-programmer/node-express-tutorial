// MODULES
// Modules are encapsulated code which share minimum stuff
const names = require('./03-names');
const greeting = require('./04-utils');
const data = require('./05-alternative-syntax')
require('./06-mindGrenade');

// console.log(names);
// console.log(data);

// greeting(names.john)