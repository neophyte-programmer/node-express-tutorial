// There is no window object in Node

//  GLOBALS
// They can be accessed anywhere in the application

//  __dirname => gives path to current directory
//  __filename => file name
//  require => function to use modules
//  module => gives info about current module/file
//  process => gives info about environment where program is being executed

// console.log(__dirname);

setInterval(() => {
    var number = 0
    console.log(number)
}, 1000)